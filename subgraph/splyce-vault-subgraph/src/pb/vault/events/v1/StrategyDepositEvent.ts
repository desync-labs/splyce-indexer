// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0

import { Writer, Reader } from "as-proto/assembly";

export class StrategyDepositEvent {
  static encode(message: StrategyDepositEvent, writer: Writer): void {
    writer.uint32(10);
    writer.bytes(message.accountKey);

    writer.uint32(16);
    writer.uint64(message.amount);

    writer.uint32(24);
    writer.uint64(message.totalFunds);
  }

  static decode(reader: Reader, length: i32): StrategyDepositEvent {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new StrategyDepositEvent();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.accountKey = reader.bytes();
          break;

        case 2:
          message.amount = reader.uint64();
          break;

        case 3:
          message.totalFunds = reader.uint64();
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  accountKey: Uint8Array;
  amount: u64;
  totalFunds: u64;

  constructor(
    accountKey: Uint8Array = new Uint8Array(0),
    amount: u64 = 0,
    totalFunds: u64 = 0
  ) {
    this.accountKey = accountKey;
    this.amount = amount;
    this.totalFunds = totalFunds;
  }
}
